# SPDX-License-Identifier: MIT OR Apache-2.0

[package]
name = "moomap"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "A copy-on-write mmap cache that keeps hot pages in RAM"
license = "MIT OR Apache-2.0"
readme = "README.md"

[dependencies]
libc = "0.2"
